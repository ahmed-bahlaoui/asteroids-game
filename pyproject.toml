[project]
name = "asteroids-pygame"
version = "0.1.0"
description = "Asteroids game implemented using Pygame"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]

[dependency-groups]
dev = [
    "black>=26.5.1",
]
